const Titulo = ( { nombre } ) => {
 
    return (
        <p style={{ display: "block", color: "white", textAlign: "center", fontSize: "20px"}}>{nombre}</p>
        
      )
  
}

export default Titulo